# Autonomous DOM Agent

## Setup
1. `pnpm install`
2. Create `.env` with `GEMINI_API_KEY` or `OPENAI_API_KEY`.
3. `pnpm start`

## Architecture
- **Explorer:** Gemini Flash (Fast DOM analysis)
- **Controller:** Orchestrates moves
- **Playwright:** Headed browser execution
